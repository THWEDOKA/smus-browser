# 🚀 Запуск SMUS Browser - Пошаговая инструкция

## Проблема
Electron запускается раньше, чем Vite успевает полностью запуститься, из-за чего показывается пустое зеленое окно.

## Решение 1: Автоматический запуск с подробными логами (РЕКОМЕНДУЕТСЯ)

Используйте новый скрипт с подробными логами:

```cmd
start-dev-verbose.cmd
```

Этот скрипт:
1. Очистит предыдущие сборки
2. Соберет Electron файлы
3. Запустит Vite
4. **Дождется** когда Vite будет полностью готов (с проверкой)
5. Только потом запустит Electron

## Решение 2: Ручной запуск в два этапа (100% надежно)

### Шаг 1: Запустите Vite
Откройте **первое** окно CMD/PowerShell:

```cmd
npm run build:electron
npm run dev
```

Дождитесь сообщения:
```
  VITE v6.3.5  ready in XXX ms
  ➜  Local:   http://localhost:5173/
```

### Шаг 2: Проверьте Vite (опционально)
Откройте браузер и перейдите на http://localhost:5173/
Вы должны увидеть интерфейс SMUS.

### Шаг 3: Запустите Electron
Откройте **второе** окно CMD/PowerShell:

```cmd
npx electron dist-electron/main.js
```

Окно браузера SMUS должно открыться!

## Решение 3: Тестирование

### Проверить работает ли Vite:
```cmd
test-vite.cmd
```

Должно показать:
```
[OK] Vite is running!
```

## Возможные проблемы

### Проблема 1: "ERR_CONNECTION_REFUSED"
**Причина:** Vite не успел запуститься или не запустился вообще

**Решение:**
1. Проверьте что Vite запущен: откройте http://localhost:5173/ в браузере
2. Если Vite не открывается, проверьте логи:
   ```cmd
   npm run dev
   ```
3. Убедитесь что порт 5173 свободен:
   ```cmd
   netstat -ano | findstr :5173
   ```

### Проблема 2: "Port 5173 is already in use"
**Причина:** Vite уже запущен или порт занят другим процессом

**Решение:**
1. Найдите процесс:
   ```cmd
   netstat -ano | findstr :5173
   ```
2. Завершите его:
   ```cmd
   taskkill /F /PID <номер_процесса>
   ```

### Проблема 3: Пустое зеленое окно
**Причина:** Electron загрузился до запуска Vite

**Решение:**
1. Закройте Electron
2. Убедитесь что Vite работает (http://localhost:5173/)
3. Запустите Electron снова:
   ```cmd
   npx electron dist-electron/main.js
   ```

## Рекомендуемый порядок запуска

### Для разработки (каждый раз):

1. **Первый терминал** (оставьте открытым):
   ```cmd
   npm run build:electron
   npm run dev
   ```
   *(Vite будет работать постоянно)*

2. **Второй терминал** (можно открывать/закрывать):
   ```cmd
   npx electron dist-electron/main.js
   ```
   *(Запускаете Electron когда нужно тестировать)*

### Преимущества этого подхода:
- ✅ 100% надежность
- ✅ Быстрый перезапуск Electron
- ✅ Hot reload работает (изменения в коде автоматически обновляются)
- ✅ Видны все логи

## Автоматический запуск

Если хотите автоматический запуск в одну команду:

```cmd
start-dev-verbose.cmd
```

Скрипт автоматически:
- Соберет Electron файлы
- Запустит Vite
- Дождется полной готовности Vite
- Запустит Electron

Все логи будут показаны в консоли.
