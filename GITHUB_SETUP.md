# 📤 Инструкция по выкладке на GitHub

Этот файл содержит пошаговые инструкции по выкладке проекта SMUS Browser на GitHub.

## ✅ Что уже готово

- ✅ `.gitignore` - обновлён, исключает лишние файлы
- ✅ `README.md` - красивый и информативный
- ✅ `LICENSE` - MIT лицензия
- ✅ `package.json` - содержит всю необходимую информацию
- ✅ Исходный код - чистый и документированный

## 🚀 Шаги для выкладки

### 1. Создать репозиторий на GitHub

1. Перейди на [github.com](https://github.com)
2. Войди в свой аккаунт
3. Нажми на иконку профиля → **New repository**
4. Заполни информацию:
   - **Repository name**: `smus-browser`
   - **Description**: `SMUS - быстрый браузер с открытым исходным кодом`
   - **Visibility**: Public (публичный)
   - **Initialize this repository with**: НЕ отмечай (у нас уже есть .gitignore и README)
5. Нажми **Create repository**

### 2. Инициализировать Git локально

```bash
cd c:\smusbrowser

# Инициализируем git
git init

# Добавляем все файлы
git add .

# Создаём первый коммит
git commit -m "Initial commit: SMUS Browser v1.0.0"

# Добавляем удалённый репозиторий (замени USERNAME на свой)
git remote add origin https://github.com/USERNAME/smus-browser.git

# Переименовываем ветку в main (если нужно)
git branch -M main

# Пушим в GitHub
git push -u origin main
```

### 3. Добавить темы (Tags)

```bash
# Добавь тег версии
git tag -a v1.0.0 -m "Release version 1.0.0"
git push origin v1.0.0
```

### 4. Добавить описание репозитория

На странице GitHub репозитория:
1. Нажми на иконку ⚙️ (Settings)
2. В разделе **About** нажми на иконку редактирования
3. Добавь:
   - **Description**: `Fast, secure and beautiful open-source browser`
   - **Website**: (если есть)
   - **Topics**: `browser` `electron` `react` `typescript` `open-source`
4. Нажми **Save**

## 📋 Проверка перед выкладкой

- ✅ Все файлы в `.gitignore`? (посмотри файл)
- ✅ README.md красивый? (уже создан)
- ✅ LICENSE файл есть? (уже создан)
- ✅ `node_modules` и `dist` в .gitignore? (да)
- ✅ package.json правильный? (да)
- ✅ Нет конфиденциальных данных в коде? (проверь)

## 🔧 После выкладки

### Настроить GitHub Pages (опционально)

Если хочешь демо сайта:
1. Settings → Pages
2. Source: Deploy from a branch
3. Branch: main, folder: /docs (если есть)

### Настроить GitHub Actions (опционально)

Для автоматических сборок:
1. Создай папку `.github/workflows`
2. Создай файл `build.yml` для CI/CD

### Запросить финансирование (опционально)

1. Settings → Sponsorships
2. Подключи свои способы получения донатов

## 📚 Дополнительно

### Полезные ссылки в README

Убедись, что в README указаны ссылки:
- `https://github.com/USERNAME/smus-browser` - главная страница
- `https://github.com/USERNAME/smus-browser/issues` - для багов
- `https://github.com/USERNAME/smus-browser/discussions` - для обсуждений

Замени `USERNAME` на свой GitHub никнейм!

### Первые коммиты

После выкладки рекомендуем:
1. Создать ветку для разработки: `git checkout -b develop`
2. Делать Pull Requests для новых фич
3. Использовать semantic commits:
   - `feat:` для новых функций
   - `fix:` для исправления багов
   - `docs:` для документации
   - `style:` для стилей
   - `refactor:` для рефакторинга
   - `test:` для тестов
   - `chore:` для других изменений

## ✨ Готово!

Теперь ваш SMUS Browser доступен всем на GitHub! 🎉

Благодарим за внесение вклада в open source сообщество!
