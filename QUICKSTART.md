# Быстрый старт SMUS Browser

## Шаг 1: Установка зависимостей

```bash
npm install
```

Это установит все необходимые пакеты (React, Electron, Vite, и др.)

## Шаг 2: Запуск в режиме разработки

### Вариант 1: Windows (рекомендуется)

Откройте PowerShell или CMD в папке проекта и запустите:

```cmd
start-dev.cmd
```

Или просто дважды кликните на файл `start-dev.cmd` в проводнике Windows.

### Вариант 2: Linux/macOS

```bash
./start-dev.sh
```

### Вариант 3: Через npm (если есть графический сервер)

```bash
npm run electron:dev
```

Эта команда:
1. Скомпилирует Electron файлы (main.ts, preload.ts)
2. Запустит Vite dev server на порту 5173
3. Откроет окно браузера SMUS

**Важно для WSL пользователей:** Electron не запустится в WSL без X-сервера. Используйте `start-dev.cmd` из Windows.

## Шаг 3: Разработка

После запуска вы увидите окно браузера с:
- Главной страницей SMUS
- Вкладками сверху
- Кнопками навигации (назад, вперед, перезагрузка)
- Поисковой строкой
- Избранными вкладками (6 слотов)

### Как использовать:

1. **Поиск**: Введите URL или запрос в поисковую строку и нажмите Enter
2. **Вкладки**: Кликните на вкладку чтобы переключиться, кликните X чтобы закрыть
3. **Новая вкладка**: Кликните на + справа от вкладок
4. **Навигация**: Используйте кнопки ←, →, ⟳ для навигации
5. **Избранное**: Кликните на избранную вкладку чтобы открыть сайт

## Шаг 4: Сборка для Windows

```bash
npm run build:win
```

Результат будет в папке `release/` :
- SMUS-Browser-Setup-1.0.0.exe (инсталлятор)
- SMUS-Browser-1.0.0.exe (portable версия)

## Возможные проблемы

### Ошибка "Cannot find module"
```bash
npm install
npm run build:electron
```

### Ошибка "Port 5173 is already in use"
Закройте другие процессы использующие этот порт или измените порт в vite.config.ts

### Ошибка при сборке
Убедитесь что установлены все зависимости:
```bash
rm -rf node_modules package-lock.json
npm install
```

## Структура проекта

```
smusbrowser/
├── electron/              # Главный процесс Electron
│   ├── main.ts           # Управление окнами и вкладками
│   └── preload.ts        # API мост между процессами
├── src/                  # React UI
│   ├── App.tsx           # Главный компонент
│   ├── main.tsx          # Точка входа
│   └── types/            # TypeScript типы
├── dist-electron/        # Скомпилированные Electron файлы
├── dist/                 # Сборка React
└── release/              # Готовые .exe файлы
```

## Следующие шаги

1. Добавить настройки браузера
2. Реализовать управление избранным
3. Добавить историю посещений
4. Добавить закладки
5. Горячие клавиши

Удачи в разработке! 🚀
